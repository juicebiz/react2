/* export default function useForm(fn, initial, onDone){


  return { 
    form,
    errors,
    pending,
    notice,
    send,
    update
  };
}
 */
/*
  { pending && <Loader /> }
   <button onClick={send}>
   <input value={form.name} onChange={e => update('name', e.t.v)}
   { errors.name ?? null }
    { notice && alert-danger<notice> }

*/